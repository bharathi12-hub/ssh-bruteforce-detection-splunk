# Splunk UDP input — receives Syslog from Metasploitable 2 on UDP/514.
# Create the ssh_bruteforce index first (Settings > Indexes) and open UDP 514
# on the Splunk host firewall.

[udp://514]
sourcetype = syslog
index = ssh_bruteforce
connection_host = ip
